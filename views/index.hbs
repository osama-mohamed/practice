<h1>{{ title }}</h1>
<p>Welcome to {{ title }}</p>

{{# if condition}}
    condition is true
{{ else }}
    condition is false
{{/ if }}

{{# each array }}
    {{ this }}
{{/ each }}

{{# each array as |val key| }}
    <br>
    {{ key }} - {{ val }}
{{/ each }}