extends layout

block content
    h1 #{title}
    ul.list-group
        each article, index in articles
            li.list-group-item
                a(href="/article/" + article._id)= article.title
